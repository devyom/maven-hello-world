pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps {
                git 'https://github.com/devyom/maven-hello-world.git'
                   withMaven(maven : 'M2') 
                      sh 'mvn clean install' 
            }
          }
        }
       
